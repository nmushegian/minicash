{
  "name": "minicash",
  "version": "0.0.2",
  "type": "module",
  "scripts": {
    "setup": "git submodule update --init --recursive",
    "build": "npx tsc",
    "pretest": "npm run build",
    "test": "node dist/test/test.js"
  },
  "files": [
    "core/*.ts",
    "test/*.ts"
  ],
  "dependencies": {
    "coreword": "^0.0.8",
    "rlp": "^3.0.0",
    "typescript": "^4.7.2"
  },
  "devDependencies": {
    "@types/node": "^17.0.38",
    "tapzero": "^0.6.1"
  }
}
